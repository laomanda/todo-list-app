name: daily_work_2026_manager
description: "A professional, responsive daily task manager for mobile, web, and desktop."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  csv: ^6.0.0
  file_picker: ^11.0.2
  font_awesome_flutter: ^11.0.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
